---
title: بررسی Context Window در LLM ها
date: 2026-07-03
description: هر LLM میتونه تعداد مشخصی از توکن هارو در لحظه ببینه و در حافظه اش نگه داره، اینجا یاد میگیری که این توکن ها چطور در context window ذخیره میشن.
series: llm-fundamentals
part: 3
seriesLabel: LLM Fundamentals
banner: /assets/banners/llm-fundamentals-3.avif
---
تا اینجای کار فهمیدیم توکن ها چی هستن و `LLM` ها چطور حرف مارو متوجه میشن.

حالا هر `LLM` میتونه تعداد مشخصی از توکن هارو در لحظه ببینه و در حافظه اش نگه داره، و به مجموع توکن هایی که یک `LLM` میتونه در حافظه اش نگه داره، `context window` گفته میشه.

این شامل `system prompt` و همه `input tokens` و `output tokens` هایی هست که حین تعامل با `LLM` تولید میشن.

![[Pasted image 20260709030251.png]]

به عبارتی، `context window` همون مجموع توکن های ورودی و خروجیه.

حالا هرچی گفت و گوی شما با `LLM` طولانی تر بشه و مسج های بیشتری ردو بدل بشه، تعداد توکن ها هم همینجور زیاد و زیادتر میشه.

![[Pasted image 20260709030307.png]]

خب ما که همینجوری نمیتونیم توکن هارو تو حلق `LLM` فرو کنیم و قطعا به لیمیت میخوریم، و در واقع هر `LLM` یک لیمیت مشخصی داره و فقط یک تعداد مشخصی از توکن هارو میتونه هندل کنه.

پس مثلا وقتی شما میخونی که کانتکست ویندوی یک مدلی `200k` هست، یعنی اون مدل میتونه `200k` توکن رو هندل کنه و اگه شما از لیمیت عبور کنی به ارور میخوری.

حالا در ادامه ما میریم و این لیمیت رو بررسی میکنیم.

## لیمیت Context Window

شما فرض بگیر که یه چت خیلی طولانی با `LLM` داری و بالاخره یکجا به لیمیت میخوری و ازونجا به بعد `API` به شما ارور برمیگردونه:

![[Pasted image 20260709030324.png]]

اصلا امکان داره که وسط صحبت و فرستادن مسج به این لیمیت بخوری، چون قبل اینکه مسج رو بفرستی همه چی اوکی بوده ولی دقیقا همون لاکار، لیمیت رو رد کردی و به ارور خوردی.

یعنی این `Context Window` اصلا با کسی شوخی نداره.

![[Pasted image 20260709030338.png]]

خود مدل هم اونقدری زبل نیست که بتونه این لیمیت رو دور بزنه، پس معمولا شما بالاخره یجایی به این ارور برخورد میکنی.

ولی حتی اگه `context window` اون `LLM` خیلی زیاد باشه هم بازم احتمالا به مشکل `Lost in the Middle` برخورد میکنی، یعنی هرچی زمان بیشتری با مدل چت میکنی احساس میکنی که داره خنگ تر میشه و صحبتای قبلی رو یادش میره.

حالا چرا این اتفاق میفته؟ الان برات بازش میکنم.

## مشکل Lost in the Middle

تا اینجا فهمیدیم که یه مدل میتونه تعداد معینی از توکن رو هندل بکنه که بهش میگن `context window`، حالا شاید یک مدلی بیاد و بگه آقا من میتونم یک میلیون توکن رو هندل بکنم.

ما هم خوشحال و خندان میگیم خب ایول مشکل حل شد، ولی واقعیت چیز دیگه ایه!

یه دقه این عکسو نگاه کن با دقت:

![[Pasted image 20260709030349.png]]

تو این عکس، هرکدوم ازین حلقه رنگی رنگیا یک مسج و ریسپانس بین کاربر و `LLM` عه.

حالا مسج هایی که اول `conversation` ایجاد شدن یه تاثیر عمده ای روی `output` دارن، اون هایی که آخر سر ایجاد شدن هم یک تاثیر عمده ای روی `output` میذارن، ولی `LLM` بیخیال اون وسطیا میشه و توجه کمتری بهشون میکنه، در نتیجه شما حس میکنی که `LLM` فراموش کار شده یا خنگ شده و میری یک چت جدید ایجاد میکنی.

این مشکل که همه `LLM` ها هم باهاش دست به گریبان هستن، به اسم `Lost in the Middle` شناخته میشه و هر چقدر `context window` بزرگتر بشه، این مشکل هم شدید تر میشه.

پس شما سری بعد که یه مدل با کانتکست ویندوی یک میلیونی دیدی، باید کمتر ذوق زده بشی چون احتمالا هرچقدر توکن بیشتری بریزی تو حلقش، خنگ تر میشه.

و حتی اگر کانتکست ویندوی مدلت هم خیلی گندس، بازم بهتره که حواست به تعداد توکنا باشه، چون توکن کمتر یعنی نتیجه بهتر!

منبع: [AI Hero](https://www.aihero.dev/what-is-the-context-window)