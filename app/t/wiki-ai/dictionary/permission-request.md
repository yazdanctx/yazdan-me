---
farsi-title:
english-title: Permission request
description: این Permission Request یعنی که Harness قبل از انجام کارای حساس، میاد ازت یه اجازه ای میگیره.
category: tools-and-environment
order: 31
---

`Permission Request` همون پیغامیه که `Harness` قبل از اجرای بعضی `Tool`ها بهت نشون میده و ازت اجازه می‌گیره.

وقتی `Model` یه `Tool Call` تولید می‌کنه، `Harness` اول بررسی می‌کنه که اجرای اون `Tool` مجازه یا نه.

اگه از قبل اجازه داشته باشه، همون لحظه اجراش می‌کنه ولی اگه نیاز به تأیید داشته باشه، همه چیز `stop` میشه و همونجا از کاربر سوال می‌پرسه.

اون موقع شما سه انتخاب داری. یا فقط همون یک بار اجازه بدی، یا بگی تا آخر این `Session` هر بار از همین نوع درخواست اومد دیگه نپرسه، یا کلاً درخواست رو رد کنی.

اگه شما درخواست رو رد کنی، `Harness` این رد شدن رو به صورت یه `Tool Result` برای `Model` می‌فرسته. مدل هم متوجه میشه این راه مجاز نبوده و سعی می‌کنه یه روش دیگه پیدا کنه یا باز ازت سوال بپرسه.

جالب اینجاست که موقع رد کردن درخواست، معمولا می‌تونی یه توضیح هم بنویسی. مثلاً بگی این دستور رو اجرا نکن، یا ازین اسکریپت استفاده کن و غیره، و خب این مسج هم به مدل می‌رسه و بهش کمک می‌کنه که دفعه‌ بعد تصمیم بهتری بگیره.

البته این قابلیت یه داستانی هم داره و تا وقتی خودت یه جواب ندی، ایجنت دیگه نمی‌تونه ادامه بده و مثلا اگه شما پشت سیستم نباشی و یه `Permission Request` بیاد، ایجنت همونجا متوقف میشه تا شما برگردی پای سیستم.

و حلا به همین خاطر معمولا `Permission Mode` رو طوری تنظیم می‌کنن که فقط کارهای حساس نیاز به تأیید داشته باشن و بقیه‌ی `Tool`ها بدون معطلی اجرا بشن.
